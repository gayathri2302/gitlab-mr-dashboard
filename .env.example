# ── Backend API URL ───────────────────────────────────────────────────────────
# In dev this is not needed — Vite proxies /api and /auth to localhost:3001
# In production set this to your deployed gitlabservice URL (no trailing slash)
VITE_API_URL=https://your-gitlabservice-url.com
